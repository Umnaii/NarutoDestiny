# BUGS.md — Bugs détectés

| # | Fichier source | Description | Gravité | Découvert à l'étape | Fix appliqué |
|---|---------------|-------------|---------|---------------------|--------------|
| 1 | js/ui.js | `showGameOver()` n'est jamais appelée. `spinCurrent()` détecte `combatResult.gameOver` et fait un simple `return` sans jamais afficher l'overlay `#goOv` — l'écran de Game Over ne s'affiche donc jamais au joueur. | ✅ CORRIGÉ | A5 (audit statique) | Dans `js/ui/ui-round.js` → `spinCurrent()`, remplacé `if (combatResult.gameOver) return;` par `if (combatResult.gameOver) { setTimeout(showGameOver, 900); return; }` (délai aligné sur la durée du flash de défaite `.defeat-flash`). |
| 2 | js/ui.js | `spinAll()` n'est jamais appelée (aucun bouton ne l'invoque) et référence `$("allBtn")`, un élément qui n'existe pas dans le HTML actuel — lèverait une exception si jamais invoquée. | ✅ CORRIGÉ | A5 (audit statique) | Supprimé `spinAll()` de `js/ui/ui-round.js` (code mort inatteignable, référence à un élément DOM inexistant) et retiré de la liste `@exports` du header du fichier. |
| 3 | js/ui.js | `sanitize()` est définie (avec commentaire "RÈGLE 1") mais jamais appelée nulle part dans le fichier. | ✅ CORRIGÉ | A5 (audit statique) | Supprimé `sanitize()` de `js/ui/ui-core.js` (code mort inatteignable) et retiré de la liste `@exports` du header ainsi que de la description du module dans `ARCHITECTURE.md`. |
| 4 | js/ui.js | `checkPromotion()` a un corps vide et n'est jamais appelée. | ✅ CORRIGÉ | A5 (audit statique) | Supprimé `checkPromotion()` de `js/ui/ui-overlays.js` (code mort, corps vide) et retiré de la liste `@exports` et de la description du header du fichier. |
| 5 | js/wheel.js | `WheelEngine.drawIssue()` est exportée mais jamais utilisée par ui.js (qui appelle `WheelEngine.draw` directement pour la roue Issue initiale). | ✅ CORRIGÉ | A5 (audit statique) | Supprimé `drawIssue()` de `js/wheel.js` (code mort inatteignable) et retiré de l'objet retourné par l'IIFE `WheelEngine`, de la liste `@exports` du header, et des références dans `ARCHITECTURE.md`. |
| 6 | js/data.js | `STYLE_TRIANGLE` est définie mais jamais référencée — le triangle de faiblesses est recodé en dur via `weakness`/`resistance` dans `computeIssueWeights()`/`getAntagData()`. | ✅ CORRIGÉ | A5 (audit statique) | Supprimé `STYLE_TRIANGLE` de `js/data.js` (code mort inatteignable) et retiré de la liste `@exports` du header du fichier. |
| 7 | css/illustrations.css | Classes `.bc-0`..`.bc-6`, `.rank-genin/-chunin/-jonin/-kage`, `.rarity-*`, `.outcome-victory/-hard/-draw/-defeat` : aucune n'est appliquée par le JS actuel (dupliquées/obsolètes par rapport à `data.js` et aux classes réellement utilisées `out-v/out-d/out-x`). | ✅ CORRIGÉ | A5 (audit statique) | Supprimé ces classes de `css/components/illustrations.css` (nouvel emplacement du fichier depuis le refactoring Phase 1) : les couleurs sont en réalité injectées inline par `ui-svg.js` via `BADGE_PALETTES`/`rank.color`, et les classes de résultat réellement utilisées sont `out-v/out-d/out-x`. |
| 8 | css/style.css | `.bslot { animation: slin .5s ... }` référence une animation `slin` qui n'est définie nulle part (aucun `@keyframes slin` dans le projet) — l'entrée des badges dans le tableau de collection n'a donc aucune animation d'apparition (échoue silencieusement, pas d'erreur console). | ✅ CORRIGÉ | C (migration CSS) | Ajouté `@keyframes slin` (fondu + léger glissement vertical) dans `css/components/recap.css` (nouvel emplacement depuis le refactoring Phase 1), juste après `.bslot` — restaure l'animation d'entrée déjà pilotée en décalage par `ui-recap.js` (`sl.style.animationDelay`). |
| 9 | js/data.js | `PERIODES[i].cssClass` (ex: `"pc-classique"`) est défini pour chaque période et les classes correspondantes existent dans `css/components/illustrations.css`, mais `ui-village.js` → `buildVillageScreen()` construit les cartes uniquement à partir de `VILLAGES` et n'applique jamais `cssClass` sur aucun élément — ces classes CSS ne sont donc jamais posées dans le DOM. | ✅ CORRIGÉ | Phase 2 (documentation JSDoc de data.js) | Supprimé le champ `cssClass` de chaque entrée de `PERIODES` dans `js/data.js` (et sa ligne JSDoc), ainsi que les classes `.pc-*` et l'effet `::after` d'accent qui en dépendaient exclusivement dans `css/components/illustrations.css` (l'accent n'était jamais visible : `--pc-accent` n'était jamais posé sur aucun élément du DOM). Le style réel des cartes village/période (bordure, survol, sélection) vit dans `css/components/village.css` et n'est pas affecté. |
